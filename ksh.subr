#!/bin/ksh

got-push() {
	local r=$(got info | awk '/^repository: / { print $2 }')
	[ -z "$r" ] && return 1
	(cd "$r" && git push "$@")
}

sloc-go() {
    find . -name '*.go' | xargs egrep -v '^[:space:]*($|(//))' | wc -l | tail -1
}
