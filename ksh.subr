#!/bin/ksh

got-push() {
    	local r=$(got info | perl -nE 'say $1 if /^repository: (.*)/')
	[ -z "$r" ] && return 1
	(cd "$r" && git push "$@")
}

sloc-go() {
    find . -name '*.go' | xargs egrep -v '^[:space:]*($|(//))' | wc -l | tail -1
}
